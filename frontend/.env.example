# Base URL of your FastAPI backend. client.js sends requests directly here.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Only used if you switch to the /api proxy in vite.config.js instead.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
